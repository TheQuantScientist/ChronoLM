[project]
name = "chronolm"
version = "0.1.0"
description = "Small language model experiments for irregular clinical time series forecasting."
requires-python = ">=3.10"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
